requests>=2.31
PyYAML>=6.0
scikit-learn>=1.3
numpy>=1.24
pypdf>=4.0
tabulate>=0.9

# Optional, only needed for --scorer claude:
# anthropic>=0.40

# Optional, only needed for --scorer groq (free tier, no card required —
# get a key at console.groq.com):
# groq>=0.11

# Optional, only needed for real (non dry-run) Supabase writes:
# supabase>=2.4
